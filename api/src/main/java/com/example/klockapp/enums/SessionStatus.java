package com.example.klockapp.enums;

public enum SessionStatus {
    /** User has at least one open or completed movement today[cite: 47]. */
    ACTIVE,

    /** The workday has been finalized (all events closed). */
    COMPLETED
}
